This is webadmin version of wicd-php-gui.

To make standalone version, `chmod 755 remove-webadmin-code.sh`, 
run `remove-webadmin-code.sh` and then `download-jquery.sh`.
After this, copy all files from this directory.

NEVER RUN `remove-webadmin-code.sh` IN PRODUCTION!!!
